<Window x:Class="HydroVue5_monitoring.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        Title="PL-MTI Monitoring - HygroVue 5" Height="620" Width="1020"
        Closed="Window_Closed">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!--controles et terminal -->
        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <GroupBox Grid.Row="0" Header="Panneau de controle" Padding="8">
                <StackPanel>
                    <TextBlock Text="Port COM" Margin="0,4"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox x:Name="cmbPorts" Grid.Column="0" Height="26"/>
                        <Button x:Name="btnRefresh" Grid.Column="1" Content="Rafraichir" Margin="6,0,0,0" Padding="8,0" Click="btnRefresh_Click"/>
                    </Grid>

                    <TextBlock Text="Frequence d'acquisition (secondes, min 5)" Margin="0,10,0,4"/>
                    <TextBox x:Name="txtFrequency" Text="5" Height="26" TextChanged="txtFrequency_TextChanged"/>

                    <Button x:Name="btnConnect" Content="Connecter" Height="30" Margin="0,10,0,0" Click="btnConnect_Click"/>

                    <Grid Margin="0,6,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="btnStart" Grid.Column="0" Content="Demarrer" Height="30" Margin="0,0,3,0" Click="btnStart_Click"/>
                        <Button x:Name="btnStop" Grid.Column="1" Content="Arreter" Height="30" Margin="3,0,0,0" Click="btnStop_Click"/>
                    </Grid>

                    <Button x:Name="btnExport" Content="Exporter en CSV" Height="30" Margin="0,6,0,0" Click="btnExport_Click"/>

                    <TextBlock x:Name="txtStatus" Text="En attente" Margin="0,10,0,0" FontWeight="Bold"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Grid.Row="1" Header="Terminal SDI-12" Margin="0,10,0,0">
                <RichTextBox x:Name="txtLog" IsReadOnly="True" Background="#FF1E1E1E"
                             VerticalScrollBarVisibility="Auto" FontFamily="Consolas">
                    <FlowDocument>
                        <Paragraph x:Name="logParagraph"/>
                    </FlowDocument>
                </RichTextBox>
            </GroupBox>
        </Grid>

        <!--valeurs et graphique -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" BorderBrush="#FFDDDDDD" BorderThickness="1" Margin="0,0,5,0" Padding="14">
                    <StackPanel>
                        <TextBlock Text="Temperature actuelle" Foreground="Gray"/>
                        <TextBlock x:Name="txtTemperature" Text="-- &#176;C" FontSize="34" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" BorderBrush="#FFDDDDDD" BorderThickness="1" Margin="5,0,0,0" Padding="14">
                    <StackPanel>
                        <TextBlock Text="Humidite relative" Foreground="Gray"/>
                        <TextBlock x:Name="txtHumidity" Text="-- % RH" FontSize="34" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </Grid>

            <GroupBox Grid.Row="1" Header="Historique des mesures" Margin="0,10,0,0">
                <lvc:CartesianChart x:Name="chart"/>
            </GroupBox>
        </Grid>
    </Grid>
</Window>